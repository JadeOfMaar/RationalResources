// Alternate means for detecting system replacers and not copying nodes for the stock planets.
// This is for RR alone to use.
// Planet mods (other than any given here) should still use the `:FOR[RationalREsourcesAU]` command

@PLANETARY_RESOURCE:HAS[#Tag]:NEEDS[GPP|GEP_Primary|BeyondHome|RealSolarSystem]:FIRST 
{ 
	NotStock = True 
}
